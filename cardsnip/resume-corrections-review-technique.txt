Resume - Corrections suite review technique

Objectif
--------

Traiter les problemes techniques signales dans la review sans lancer de refonte lourde :

- pas de pagination API ;
- pas de gestion 404 API claire ;
- pas d'index SQL FK suffisants ;
- pas de .gitignore Python local dans scraper/ ;
- pas de loading/error boundary React app-wide ;
- risque de regression apres changements Python/Next.


Corrections effectuees
----------------------

1. Pagination API

Fichiers :

- scraper/api.py
- scraper/storage/repositories.py

Endpoints listes acceptent maintenant :

- limit
- offset

Endpoints concernes :

- GET /products
- GET /shops
- GET /tracked-products
- GET /observations
- GET /alerts

Protection ajoutee :

- limit minimum : 1
- limit maximum : 200
- offset minimum : 0


2. Gestion 404 API

Fichiers :

- scraper/api.py
- scraper/storage/repositories.py

Endpoints ajoutes :

- GET /products/{product_id}
- GET /shops/{shop_id}
- GET /tracked-products/{tracked_product_id}

Comportement :

- Produit introuvable -> 404 avec message clair
- Boutique introuvable -> 404 avec message clair
- Produit suivi introuvable -> 404 avec message clair

Creation de suivi :

POST /tracked-products verifie maintenant explicitement :

- product_id existant ;
- shop_id existant.

En cas d'id invalide, l'API renvoie une erreur claire au lieu d'une erreur opaque SQLite.


3. Index SQL

Fichier :

- scraper/storage/schema.sql

Index ajoutes :

- tracked_products_product_idx
- tracked_products_shop_idx
- price_observations_stock_idx
- alerts_tracked_type_idx

Index deja presents et conserves :

- tracked_products_active_idx
- price_observations_tracked_checked_idx
- alerts_created_idx


4. .gitignore Python local

Fichier ajoute :

- scraper/.gitignore

Ignore explicitement :

- __pycache__/
- *.py[cod]
- .pytest_cache/
- .mypy_cache/
- .ruff_cache/
- .venv/
- *.log

Note :

Le .gitignore racine couvrait deja Python, mais ce fichier local rend la protection explicite dans scraper/.


5. Loading et Error Boundary React

Fichiers ajoutes :

- web/src/app/loading.tsx
- web/src/app/error.tsx

Objectif :

- etat de chargement app-wide avec style CardSnip ;
- erreur React app-wide avec bouton Reessayer ;
- eviter les crashs silencieux sur les routes Next.js.


Non traite volontairement dans cette passe
------------------------------------------

Refactorisation des gros composants :

- cardsnip-dashboard.tsx (~584 lignes)
- catalogue-page.tsx (~563 lignes)

Raison :

Ce sont de vrais sujets de maintenabilite, mais les decouper demande une passe separee pour eviter de casser l'UI et les flux deja valides.

Recommandation :

Faire ensuite une passe dediee :

- extraire les mocks/data deja separes ;
- extraire les panels metiers ;
- extraire les modals/formulaires ;
- garder les composants de page comme orchestrateurs.


Tests effectues
---------------

Python py_compile :

OK

Tests unitaires Python :

OK - 3 tests

Init SQLite :

OK

npm lint :

OK

npm build :

OK

Test end-to-end local :

OK

Flux valide :

fake-shop -> scraper -> SQLite -> FastAPI -> dashboard/API

Verification API pagination / 404 :

OK

GET /products?limit=1&offset=0 :

200

GET /products/999999 :

404 - Produit introuvable

GET /shops/999999 :

404 - Boutique introuvable


Conclusion
----------

Les correctifs de stabilisation backend/UX transverses sont appliques.

Le flux MVP local reste stable.

Le chantier restant principal est la refactorisation progressive des gros composants frontend, a faire dans une passe separee.
